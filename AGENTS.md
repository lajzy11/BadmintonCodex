# 羽點通開發規範

## 規格優先順序

- 現行產品需求以 `docs/product/羽點通_PRD_V2.4.md` 為唯一正式 PRD。
- `docs/archive`、`prototype` 與研究文件只供歷史參考；與現行 PRD 衝突時不得採用。
- UI 局部修正應先閱讀 PRD 14.15 驗收清單與本次變更直接相關的第 14 章小節；新增頁面、跨頁重構或整體視覺調整才需完整閱讀第 14 章。
- 若新需求與第 14 章衝突，先更新 PRD 並取得設計決策，再進行程式實作；不得建立未記錄的單頁例外。

## UI 實作最低要求

- 重用 `src/styles/global.css` 的語意 Token 與共用元件，不在頁面新增品牌綠色、獨立版寬或重複的按鈕／輸入框規格。
- 登入後桌機頁面的實際內容最大寬度統一為 1040px。
- 綠色只表示成功／完成狀態；主要 CTA、選取、Focus 與連結使用藍色。
- 一般表單維持「欄位名稱＋紅色必填星號／輸入控制」兩行結構，Placeholder 遵循 PRD 14.10。
- 卡片可用於劃分大型功能區塊、摘要模組與具有獨立操作或狀態邊界的單位；區塊內以排版、間距、資料列、表格與必要分隔建立層級，避免相同重量的容器層層嵌套。
- 間距、文字層級、CTA 層級與響應式驗收依 PRD 14.8–14.14 執行。

## 完成前檢查

- 驗證範圍依變更風險分級，不為文件、文案或單一局部修正固定執行全部命令：
  - 文件或純文案：檢查差異與格式即可。
  - 局部程式修改：執行 `pnpm typecheck` 與直接相關測試；涉及格式或規則時再執行 `pnpm lint`。
  - 跨模組、依賴、建置設定、資料庫契約、發布前或使用者明確要求完整驗收：執行 `pnpm typecheck`、`pnpm lint`、`pnpm test` 與 `pnpm build`。
- 涉及 UI 行為或版面的變更，至少檢查 360px 手機、768px 平板與 1280px 桌機，不得依賴 Hover 才能完成核心操作；非 UI 變更不需重複進行三種寬度驗收。
